## Test environments

* R 4.6.1, macOS Tahoe 26.4, aarch64-apple-darwin23
* win-builder (R-devel), R-release

## R CMD check results

There were 0 errors, 0 warnings, 0 notes.

Previous submission had a NOTE for slow `dataset()` examples on the
incoming pretest; those examples are now wrapped in \\dontrun{}.

## Downstream dependencies

No reverse dependencies.
